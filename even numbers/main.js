const number = 91; // Initialize a simple int variable

if (number % 2 === 0) { // add if else to check whether is it divisible by 2
    console.log(`${number} is an even number`); // log that it is an even number
} else {
    console.log(`${number} is an odd number`); // log that it is an odd number
}

